// Compose 编译器稳定性声明（审计 P1·2026-07-02）——让下列跨模块边界的类被视为 stable，
// MessageRow 等以它们为参数的 composable 才能按 equals 做行级 skip（否则退化为实例 === 比较，
// Room 每次重查询产出新实例 → 全部可见行无谓重执行）。
// 契约：列入此表的类必须全字段 val 且 equals 覆盖全部 UI 可见字段（MessageEntity 已升级全字段 equals·见其注释）。
com.situ.aichat.data.local.entity.MessageEntity
